const create = ({username, password}) => {
  return `Welcome to Stomatic Clot. You\'re on foldername/modules/methods. And has username: ${username} and password: ${password}`
}

module.exports = {
  create
}
